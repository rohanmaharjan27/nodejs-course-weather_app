<!DOCTYPE html>
<html>
    <head>
        <title>NodeJS Web Server</title>
        <link rel='stylesheet' href="/css/styles.css">
    </head>
    <body>
        {{>header}}
        <p>{{errorMessage}}</p>
        {{>footer}}
    </body>
</html>